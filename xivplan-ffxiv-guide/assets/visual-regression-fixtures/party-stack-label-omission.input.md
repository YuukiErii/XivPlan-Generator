# Party Stack Label Omission Fixture

Test the Phase R/S rule for stack and overlap frames.

- Normal spread frames must show job icons and nearby role labels for all eight players.
- Stack/cluster frames may hide role labels and shrink job icons, but job icons must remain visible and readable.
- After the stack resolves, role labels must return in the reset/spread frame.
- The crop sheet should prove both states: full labels in normal frames, icon-only readable identity in the stack frame.

